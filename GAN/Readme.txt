GAN_Gaussian_Distribution.py doesn’t need any dataset. It generates itself.
GAN_MNIST.py loads MNIST dataset via API. So there is no need to put dataset with it.
GAN_CIFAR.py needs to placed with CIFAR-10 dataset folder. 
If you have any further questions, feel free to contact me at yingyiz2@illinois.edu.